package com.tinycraft.nativeandroid

import android.graphics.Color

object UiPalette {
    const val BG = "#101418"
    const val PANEL = "#171D22"
    const val PANEL_2 = "#202830"
    const val BORDER = "#39444D"
    const val TEXT = "#EEF3F6"
    const val MUTED = "#9AA8B2"
    const val ACCENT = "#72C94A"
    const val SKY = "#8FC7E8"

    val bg = Color.parseColor(BG)
    val panel = Color.parseColor(PANEL)
    val panel2 = Color.parseColor(PANEL_2)
    val border = Color.parseColor(BORDER)
    val text = Color.parseColor(TEXT)
    val muted = Color.parseColor(MUTED)
    val accent = Color.parseColor(ACCENT)
    val sky = Color.parseColor(SKY)
}
