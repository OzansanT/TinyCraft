Three.js r170 belongs at three.module.js in this folder.
The GitHub Actions build downloads the exact r170 module before compiling the APK.
At runtime the APK loads this local copy and requires no network access.
